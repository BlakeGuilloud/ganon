
//  returns a function that invoked with params either:
//      - invokes func with params, if params length matches func's arity
//      - a new (curried) function, accepting func's remaing parameters

function curry(func) {
  // Your code goes here
}

module.exports = curry;