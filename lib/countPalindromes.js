/**
 * Counts the number of palindromes longer than 2 characters in a string
 * @param  {String}
 * @return {Number}     Number of palindromes in `str`
 */
function countPalindromes(str) {
}

module.exports = countPalindromes;