/**
 * Returns copy of array without falsy values.
 * FYI in JS falsy values are: null, false, undefined, NaN, 0, ""
 *
 * @param {Array} arr
 * @returns {Array}
 */

function compact(arr) {
  throw new Error("This method is still a prototype");
}

module.exports = compact;
