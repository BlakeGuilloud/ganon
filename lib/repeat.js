/**
 * `repeat` returns the same value k times
 *
 * @param value: the value to be repeated
 * @param k {Integer}: number of times the value if repeated
 * @returns {Array} The resulting array of repeated values.
 */
function repeat(value, k) {
  return;
}

module.exports = repeat;
