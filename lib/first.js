/**
 * Returns the first item in a list
 *
 * @param {Array} List of things
 * @returns {Any} First Item in a list
 */
function first (arr) {
  if(arr.length === undefined){
    return arr[0];
  }
  else {
    return arr[0];
  }
}
module.exports = first;
