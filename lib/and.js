/**
 * Returns true if both arguments are true; false otherwise.
 * @param {Boolean} a
 * @param {Boolean} b
 * @return {Boolean}
 */
function and(a,b) {
// your code goes here
  return a && b;
}

module.exports = and;
