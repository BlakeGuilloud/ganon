/**
 * This method should not modify the original array
 * It will take an array an return an object
 * the keys will be the array index and value will be array[index] + 1
 * At index location 0 => 1 + 1, 1 => 2 + 1, 2 => 3 + 1, 3 => 4+1
 * [1, 2, 3, 4] => { 0: 2, 1: 3, 2: 4, 3 : 5}
 * * @param {Array} the array to be converted to object
 * @returns {Object} the computed key value pair
 */

function reduceToObject (arr) {}
module.exports = reduceToObject;