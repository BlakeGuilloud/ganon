/**
 * Returns last item in Array
 * By default, does not mutate original Array
 *
 * @param {Array|Sting} arr
 * @param {Boolean} mutate
 * @returns {*}
 */

function arrayPop(arr, mutate = false) {
  // your code here!
}

module.exports = arrayPop;
