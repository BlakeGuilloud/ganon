// Given a radius and degrees of a sector of a circle (pie slice of circle),
// return the area of the sector. Round to nearest 0.00.
// Example Radius = 5, sectorDegrees = 60, therefor area 13.09

function areaOfCircleSector(radius, sectorDegrees) {
  return new Error("Not Implimented.");
}

module.exports = areaOfCircleSector;
