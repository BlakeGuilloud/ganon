/**
 * Takes an array, and orders the numbers from least to greatest.
 * 
 * @param {Array} An array of numbers.
 *
 * @return {Array} An array of numbers ordered from least to greatest.
 * 
 */
function arrayOrder(arr) {
  // Your code here
}

module.exports = arrayOrder;
