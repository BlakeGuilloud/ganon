/**
 * Reduces list from right index to left. Like Array.prototype.reduce, but arabian.
 * @param {Array} list
 * @param {Function} iteratee
 * @param {*} memo
 * @return {*}
 */

function reduceRight(list, iteratee, memo) {
  throw new Error("Still a skeleton");
}

module.exports = reduceRight;
