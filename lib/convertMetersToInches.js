/**
 * Converts meters to inches
 *
 * @param {Number} meter
 * @returns {Number}
 */

function convertMetersToInches(meter) {
  return (meter / 0.0254)
}

module.exports = convertMetersToInches;
