/**
 * Takes an argument and returns ! of that argument.
 * eg. not(true) 
 * returns false
 * @param  {Any} value any value to be negated
 * @return {Boolean} ! of 
 */

const notnot = (value) => {
	return !!value;
};

module.exports = notnot;
