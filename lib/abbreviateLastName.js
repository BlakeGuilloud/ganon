/**
 * Write a function that accepts a first and last name and returns the abbreviated last name
 * @param {String} name
 * @return {String}
 */

const abbreviateLastName = (name) => {
  throw new Error("FIX ME");
};

module.exports = abbreviateLastName;
