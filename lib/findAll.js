/**
  Find all occurances of given element in a given array and return list of its
  indices, if it's not present return an empty array.
  @param {array} arr
  @param {*} b
  @returns {array} indices of a given parameter b in array arr
*/

function findAll(arr, b) {
  // insert your code
}
