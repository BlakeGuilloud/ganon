/**
 * Function that returns the surface area of a frustum with the given top and bottom radius
 * and height as input.
 * Depending on input of 'total' and 'lateral' it will return accordingly
 * If none is passed it will return total surface area by default
 *
 * @param {Number} radius1
 * @param {Number} radius2
 * @param {Number} height
 * @param {String} type (total or lateral)
 * @return {Number} surface area.
**/
function surfaceAreaOfFrustum(radius1, radius2, height, type) {
  throw new Error("method is still a skeleton");
}

module.exports = surfaceAreaOfFrustum;