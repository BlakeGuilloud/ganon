/*
 * Applies a function against each item in a collection from left to right and returns a new collection with the results
 * @param {Array, Object} collection
 * @param {Function} callback to be applied to items
 *
 */

function mapCollection(collection, callback) {
  throw new Error("FIXME");
}

module.exports = mapCollection;
