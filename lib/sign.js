/**
 * TODO: Write a function that returns the sign of its parameter
 * @param {Number} value The value whose sign should be determined.
 */
function sign(value) {
  return Math.sign(value);
}

module.exports = sign;
