/**
 * `isArray` returns true if the value is an array and false if the
 * value is not.
 *
 * @param {*} value Any value.
 * @returns {boolean}
 */
function isArray(value) {
  throw new Error("Method is still a skeleton");
}

module.exports = isArray;
