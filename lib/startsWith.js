/**
 * Returns true if targetString starts with substring. Otherwise, returns false.
 * @param {string} targetString
 * @param {string} substring
 * @returns {boolean}
 */

function startsWith(targetString, substring) {
  //your code
}

module.exports = endsWith;
