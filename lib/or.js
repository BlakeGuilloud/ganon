/**
 * Returns bitwise OR of all parameters.
 * Given only booleans, the result is a logical OR.
 * Parameters can be as many as you wish.
 * Parameters can be simple integers, booleans or array
 * of such types. They can even be a mix of those.
 * NOTE: Nested arrays are not supported.
 *
 * HINT: Take a look at the And, Xor and Sum methods
 */

function or(...args) {
  // write code here
}

module.exports = or;
