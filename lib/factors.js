/**
* Accepts a number and returns an array that
* contains all the prime factors of the number
* @param {Number} num The number to be factorised
* @return {Array} return an array of prime factors of input
*/
function factors(num) {
  //your code here
}

module.exports = factors;