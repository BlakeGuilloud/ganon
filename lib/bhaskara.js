/**
 * Takes a, b and c values and calculate Bhaskara's formula.
 *
 * @param {number} a smaller radius
 * @param {number} b bigger radius
 * @param {number} c height of frustum
 *
 * @return {[number, number]} Tuple of return number with delta is more than zero
 * @return {number} A unique value of bhaskara with delta is zero
 */
function bhaskara(a, b, c) {
  throw new Error("method is still a skeleton");
}

module.exports = bhaskara;
