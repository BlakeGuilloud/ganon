/**
 * Truncate a given string 's' to 'n' number of characters. 
 *      - if the magnitude of n exceeds that of string length, return an empty string.
 *      - return an empty string for invalid inputs like negative n value, s is not a string etc.
 * @param {String} s - String to be truncated
 * @param {Number} n - Max characters / Upper bound 
 * @returns {String} - Truncated string
*/

function truncate(s, n) {
  // Your code here!
}

module.exports = truncate;
